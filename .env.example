# Helper Scripts Environment Configuration

# System utilities directory
# This is the MAIN/CENTRAL directory for system utilities like theme.sh
# Default: .sys
SYS_DIR=.sys

# GitHub configuration directory
# Contains workflow templates, skip system, etc.
# Default: .github
GITHUB_DIR=.github

# Script directories to scan (comma-separated)
# Default: docker,dev,utils
SCRIPT_DIRS=docker,dev,utils
